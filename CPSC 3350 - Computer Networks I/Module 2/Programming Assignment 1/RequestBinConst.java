public interface RequestBinConst {
    String DEFAULT_ENCODING = "ISO-8859-1"; // If needed for string encoding
    int MAX_PACKET_LENGTH = 1024; // Max length on the wire, adjust as needed
    // Add other constants related to Request and Response processing
}